# Core dependencies
pyusb>=1.2.1
websockets>=12.0

# Mac menu bar (Mac only)
rumps>=0.4.0; sys_platform == 'darwin'

# Windows system tray (Windows only)
pystray>=0.19.0; sys_platform == 'win32'
pillow>=10.0.0; sys_platform == 'win32'

# Build tools
pyinstaller>=6.0.0
